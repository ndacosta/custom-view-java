<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kickingmobiledev.customviewjava">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activities.Section1Activity"
            android:label="@string/section1" />
        <activity
            android:name=".ui.activities.Section2Activity"
            android:label="@string/section2" />
        <activity
            android:name=".ui.activities.Section3Activity"
            android:label="@string/section3" />
    </application>

</manifest>